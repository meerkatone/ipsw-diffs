## UIIntelligenceIntents

> `FileSystem/System/Library/PrivateFrameworks/UIIntelligenceIntents.framework/Localizable.loctable`

```diff

 en.Precomputed Content Advisories JSON = "Precomputed Content Advisories JSON"
 en.Present Result = "Present Result"
 en.Present Writing Tools Result = "Present Writing Tools Result"
-en.Present writing tools result ${text} in ${app}. = "Present writing tools result ${text} in ${app}."
+en.Present Writing Tools result ${text} in ${app}. = "Present Writing Tools result ${text} in ${app}."
 en.Presentation Result = "Presentation Result"
 en.Preserved Ranges = "Preserved Ranges"
 en.Process Identifier = "Process Identifier"

 en.The process that will be told to insert text. = "The process that will be told to insert text."
 en.The process that will be told to run a command. = "The process that will be told to run a command."
 en.The targeted text field could not be located. The on-screen layout may have changed since it was captured. = "The targeted text field could not be located. The on-screen layout may have changed since it was captured."
-en.The text to be inserted into the app's active text field. = "The text to be inserted into the app's active text field."
+en.The text to be inserted into the app’s active text field. = "The text to be inserted into the app’s active text field."
 en.There is no text being edited in the app. Place the insertion point in a text field and try again. = "There is no text being edited in the app. Place the insertion point in a text field and try again."
 en.This operating system does not support inserting text. = "This operating system does not support inserting text."
 en.Title = "Title"

 en.When true, replaces the entire contents of the target text field. When false or unset, inserts at the current cursor. = "When true, replaces the entire contents of the target text field. When false or unset, inserts at the current cursor."
 en.When true, replaces the entire contents of the target text field. When false, replaces the current selection or inserts at cursor. = "When true, replaces the entire contents of the target text field. When false, replaces the current selection or inserts at cursor."
 en.Window Tab = "Window Tab"
-en.Window number (stringified) of the window that hosts the target text field. When provided on macOS, the bridge activates that window (`makeKeyAndOrderFront:`) before starting Writing Tools so AppKit's default `keyWindow.firstResponder` coordinator walk lands on the intended field — necessary when a transient popover has stolen key focus. = "Window number (stringified) of the window that hosts the target text field. When provided on macOS, the bridge activates that window (`makeKeyAndOrderFront:`) before starting Writing Tools so AppKit's default `keyWindow.firstResponder` coordinator walk lands on the intended field — necessary when a transient popover has stolen key focus."
+en.Window number (stringified) of the window that hosts the target text field. When provided on macOS, the bridge activates that window (`makeKeyAndOrderFront:`) before starting Writing Tools so AppKit’s default `keyWindow.firstResponder` coordinator walk lands on the intended field — necessary when a transient popover has stolen key focus. = "Window number (stringified) of the window that hosts the target text field. When provided on macOS, the bridge activates that window (`makeKeyAndOrderFront:`) before starting Writing Tools so AppKit’s default `keyWindow.firstResponder` coordinator walk lands on the intended field — necessary when a transient popover has stolen key focus."
 en.WindowTab = "WindowTab"
 en.Writing Tools = "Writing Tools"
 en.Writing Tools Context = "Writing Tools Context"

```
